# dependencies
/node_modules

# next.js
/.next
/out

# env (commit .env.example only)
.env
.env.local
.env.*.local

# deploy artifacts: proprietary source export + local src tarball (never publish)
.deploy/

# build / cache
/dist
/build
*.tsbuildinfo

# test artifacts
/coverage
/playwright-report
/test-results

# logs / os
*.log
*.err
.DS_Store

# local dev database (embedded postgres) + agent tooling
.pg-data/
.understand-anything/

# local tooling / editor config (not part of the project)
.serena/
.claude/
.idea/
.vscode/
.agents/
.continue/
.trae/
# Local IDE/tool skill installs (e.g. react-doctor). The project's own skills
# live under docs/superpowers/skills (if any); skills/ at the repo root is
# treated as local tooling.
/skills/

# NOTE: bun.lock IS committed (locked decision: bun.lock is the only lockfile).
# Do not ignore it. Other lockfiles are banned from commits.
package-lock.json
pnpm-lock.yaml
yarn.lock
